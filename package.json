{
  "name": "ecommerce-product-multi-filter-sidebar",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace client\" \"npm run dev --workspace server\"",
    "build": "npm run build --workspace client"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
